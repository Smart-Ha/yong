

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_yong?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver


thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    encoding: utf-8
    mode: HTML5
    content-type: text/html
    cache: false


# mybaits
mybatis:
    config-location: classpath:spring/mybatis-config.xml
    mapper-locations: classpath:mapper/*.xml

# log

# tomcat server port
server:
    port: 8080

logging:
  level:
    com.wangy: debug


# mail
